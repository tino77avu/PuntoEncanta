spring.application.name=PuntoEncanta

# Puerto del servidor (Render usa variable de entorno PORT)
server.port=${PORT:8080}

# Configuración de WhatsApp
app.whatsapp.number=${WHATSAPP_NUMBER:51999999999}
app.whatsapp.url=https://wa.me/${app.whatsapp.number}
